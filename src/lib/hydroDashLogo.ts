/**
 * Intrinsic geometry of `public/hydroDashLogo.svg` (root `<svg>` width/height and `viewBox`).
 * When you change the SVG file, update these values to match so <img width/height> hints stay correct.
 */
export const HYDRODASH_LOGO_SVG_WIDTH = 114.98
export const HYDRODASH_LOGO_SVG_HEIGHT = 94.747

/** Width∶height (≈ 1.214∶1 for the current asset). */
export const HYDRODASH_LOGO_ASPECT_RATIO = HYDRODASH_LOGO_SVG_WIDTH / HYDRODASH_LOGO_SVG_HEIGHT
